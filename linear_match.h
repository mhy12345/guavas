#ifndef LINEAR_MATCH

#define LINEAR_MATCH

#include <cmath>

double sqr(double x);
double dist(double x0, double y0, double x1, double y1);

namespace LinearMatch {
	double matchArrs(int*, int*, int*);
};

#endif
